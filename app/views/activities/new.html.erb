<h2><%= t(:'title.new_activity') %></h2>

<ul class="nav nav-tabs">
  <%= nav_link_to t(:'button.primary_data'), "#primary_data", :'data-toggle' => "tab", :wrap_class => "active" %>

  <%= link_to iconed(t(:'button.list'), :th_list, :white), activities_path, :class => "btn btn-primary pull-right" %>
</ul>

<%= render "form" %>