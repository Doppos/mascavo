<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Mascavo" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Mascavo is a CRM made to transform data into strategic information." %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body id="<%= params[:controller].parameterize %>">

    <header>
      <%= render "layouts/header" %>
    </header>

    <div id="main" role="main">
      <div class="container-fluid">
        <div class="content">
          <%= render "layouts/messages" %>
          <%= yield %>
        </div>
      </div>

      <footer>
        <div class="container-fluid">
          <%= render "layouts/footer" %>
        </div>
      </footer>
    </div>

  </body>
</html>