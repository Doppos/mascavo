<h2><%= t(:'title.edit_company') %></h2>

<ul class="nav nav-tabs">
  <%= nav_link_to t(:'button.primary_data'), "#primary_data", :'data-toggle' => "tab", :wrap_class => "active" %>
  <%= nav_link_to t(:'button.complementary_data'), "#complementary_data", :'data-toggle' => "tab" %>

  <div class="btn-group pull-right">
    <%= link_to iconed(t(:'button.show'), :eye_open, :white), company_path, :class => "btn btn-primary" %>
    <%= link_to iconed(t(:'button.list'), :th_list), companies_path, :class => "btn" %>
    <%= link_to iconed(t(:'button.new_company'), :plus), new_company_path, :class => "btn" %>
  </div>
</ul>

<%= render "form" %>